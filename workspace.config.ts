// There isn't really a deterministic way to resolve the content directory relative
// to the workspace root, so we provide one here.

/**
 *
 */
export const rootPath = __dirname;

export const contentDirs = ['content/data', 'content/pages']
